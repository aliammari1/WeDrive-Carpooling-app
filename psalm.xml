<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    findUnusedBaselineEntry="false"
    findUnusedCode="false"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="src" />
        <directory name="Model" />
        <directory name="Controller" />
        <ignoreFiles>
            <directory name="vendor" />
            <directory name="View" />
        </ignoreFiles>
    </projectFiles>

    <!--
        Taint analysis is the SAST that catches the auth-bypass / SQL-injection
        class of bugs (CodeQL has no PHP support). Enable in CI with:
            vendor/bin/psalm --taint-analysis --report=results.sarif
        psalm-github-actions runs this with security_analysis: true.
    -->
