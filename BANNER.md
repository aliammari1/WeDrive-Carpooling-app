# Banner & Social Preview

This file tracks the WeDrive visual identity.

## Current asset

- [`assets/banner.svg`](assets/banner.svg) — a 1280×640 **placeholder** hero.

## Concept / art direction

**Automotive route-line "W"** — a dashed road (highway dash pattern) traces the
letter **W**, with map pins at each vertex, over a teal→navy gradient (mobility +
sustainability). The wordmark "WeDrive" and the tagline sit below.

## TODO (deferred — needs the brand tooling / a designer)

- [ ] Replace the SVG placeholder with a polished render using the **`brandkit`**
      Claude skill (identity board + 1280×640 social preview) and
      **`imagegen-frontend-web`** for a wide README hero.
- [ ] Export a 1280×640 PNG and set it as the GitHub **Settings → Social preview**.
- [ ] Add product screenshots/GIFs (dashboard, ride matching) to `assets/` and
      reference them in the README screenshots section.

The README references `assets/banner.svg` as the hero today; swap the file in
place once the final render exists.
